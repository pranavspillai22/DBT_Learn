##DBT CORE INSTALLATION
--Prerequisites
  --Python
  --VS Code
  --Snowflake Trial Account

-Steps
--I had python 3.14. DBT Core is compatible with 3.12 or below. So I downgraded version to 3.12.
--VS Code Terminal - Go to Project folder
--Create Virtual Environment - python -m venv dbt-env
--Activate venv - dbt-env\Scripts\activate
--Install DBT Core and Snowflake adapter - pip install dbt-snowflake
--dbt --version (1.11.11 got installed)

##SNOWFLAKE PROJECT SETUP
--Create Database & Schema
-- Create the tables -> Following Star Schema 
  - 4 DIMENSION tables -> dim_customers, dim_products, dim_stores, dim_dates
    FACT Table -> fact_orders

##ARCHITECTURE
            {{ source() }}                    {{ ref() }}
RAW SCHEMA ----------------> STAGING SCHEMA -----------------> MARTS SCHEMA
(dim_customers               (stg_customers                    (final_cube)
 dim_products                 stg_products
 dim_stores                   stg_stores
 dim_dates                    stg_dates
 fact_orders)                 stg_orders)

##PROJECT STRUCTURE

sf_dbt_project/
|-- dbt_project.yml                 # Project configuration
|-- profiles.yml                    # Snowflake connection settings
|-- models/
│   |-- sources.yml                 # Raw source table declarations
│   |-- staging/
│   |   |-- stg_orders.sql
│   |   |-- stg_customers.sql
│   |   |-- stg_products.sql
│   |   |-- stg_stores.sql
│   |   |-- stg_dates.sql
│   |-- marts/
│       |-- final_cube.sql          # Final aggregated table


##Create a new User in Snowflake
  CREATE USER IF NOT EXISTS dbt 
      PASSWORD='dbtuser123'
      LOGIN_NAME='dbt'
      MUST_CHANGE_PASSWORD=FALSE
      DEFAULT_WAREHOUSE='COMPUTE_WH'
      DEFAULT_ROLE=ACCOUNTADMIN
      DEFAULT_NAMESPACE='DBTPROJECT.RAW';
  GRANT ROLE ACCOUNTADMIN TO USER dbt;

##VS CODE SETUP
  --In terminal, create new virtual environment under the project folder
    python -m ven sfenv
  --Activate venv -> sfenv\Scripts\activate
  --Install dbt and snowflake adapter
    pip install dbt-snowflake

##DBT INITIALIZATION
  --Created dbt project in vscode using 'dbt init'
  --This step will create the dbt project folder with necessary folders. Also dbt_project.yml and profiles.yml files will be created.
  --dbt_project.yml -> Brain of project. Central configuratiion file that tells DBT everything about the project.
  --profiles.yml -> Helps DBT to connect with Snowflake. 
  --Run 'dbt debug' to verify the connection.

##DECLARE SOURCES ('sources.yml')
  --Register raw tables so models reference them via '{{ source('raw', 'table_name') }}'
  --We can add tests to check data quality

##BUILD STAGING MODELS
  --Each staging model:
    - References source via '{{ source() }}'
    - Selects and renames relevant columns
    - Applies light transformations
    - Materialized as 'views'

##BUILD MART MODEL
  --The 'final_cube.sql' model:
    - JOINs fact table to all 4 dimensions
    - Aggregates key business metrics
    - Materialized as 'table' for fast dashboard queries

##RUN DBT (compile, run, test)
  --Run the below commands
    --dbt compile -> validates SQL without executing and not touching Snowflake
    --dbt run     -> executes against Snowflake
    --dbt test    -> Test data quality

##GENERATE DOCUMENTATION
  --dbt docs generate
  --dbt docs serve


##STREAMS
  --Stream is essentially a change data capture (CDC) mechanism. It records row-level changes (DML operations) made to a source table since the last time the stream was consumed.
  --Change Tracking: When enabled, Snowflake maintains hidden metadata about row changes. This metadata is what streams query to return only the "delta" (new/changed/deleted rows).
  --If CHANGE_TRACKING is not set to TRUE, you can still query the table normally, but streams won’t be able to capture incremental changes.
  --Create streams for all the raw tables
  --Update a record in the 'Dim_Customers' table
    --Capture the changes in streams
      --Before Updating the table
        https://github.com/pranavspillai22/DBT_Learn/blob/d14fc29c4310b51ade6b59a15ebef5a4146e598f/Before_Update_Customers_Stream.png
      --After Updating the table, in streams 2 records got populated in STREAM_STG_CUSTOMERS, one for Delete and one for Insert
        https://github.com/pranavspillai22/DBT_Learn/blob/d14fc29c4310b51ade6b59a15ebef5a4146e598f/After_Update_Customers_Stream_1.png
        https://github.com/pranavspillai22/DBT_Learn/blob/d14fc29c4310b51ade6b59a15ebef5a4146e598f/After_Update_Customers_Stream_2.png
        
        https://github.com/pranavspillai22/DBT_Learn/blob/d14fc29c4310b51ade6b59a15ebef5a4146e598f/After_Insert_Orders_Stream_1.png
        https://github.com/pranavspillai22/DBT_Learn/blob/d14fc29c4310b51ade6b59a15ebef5a4146e598f/After_Insert_Orders_Stream_2.png

  --Insert a new record in 'Fact_Orders' table
    --Before Inserting the table
      https://github.com/pranavspillai22/DBT_Learn/blob/d14fc29c4310b51ade6b59a15ebef5a4146e598f/Before_Insert_Orders_Stream.png
    --After inserting, one record got populated in STREAM_STG_ORDERS, for Insert

  --Add timestamp field on Final_Cube
    --Added 'created_at' field in the final mart table to track changes
  
